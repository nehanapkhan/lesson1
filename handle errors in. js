try {
    let result = 10 / 0; // No error, but results in Infinity
    console.log(result);
    
    let x = y + 1; // Error: y is not defined
} catch (error) {
    console.error("An error occurred:", error.message);
}
